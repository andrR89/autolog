# HOMOLOG.md — AutoLog

> Log de homologação do Diretor (André). Uma linha por homologação de sprint.
> Fluxo e regras do gate: ver `CLAUDE.md → Padrão de desenvolvimento → Quando a homologação reprova`.

| Data | Sprint | O que foi testado | Veredito | Falhas | Resolução |
|---|---|---|---|---|---|
| 2026-05-23 | Sprint 0 | Setup Supabase (SQL+RLS+redirect), build iOS simulator (com `dart_define.json`), cadastro/login email-senha, validações PT-BR (email vazio/inválido, senha curta), credenciais incorretas, persistência de sessão entre restarts, "Entrar com Google" (OAuth via navegador), logout | ✅ **Aprovado** | — | — |
| 2026-05-23 | Sprint 1 | CRUD de veículos (criar/listar/editar/soft-delete) com validações inline PT-BR; indicador de sync na AppBar; round-trip de sync (push do app, pull de linha inserida direto no Supabase); soft-delete propagado pro remoto; fluxo offline (cloud_off → retry → synced); persistência de sessão | ✅ **Aprovado** | — | — |
| 2026-05-23 | Sprint 2 | Navegação nova (tap em veículo → detalhe, pencil → editar); form de abastecimento (validações PT-BR, parser decimal pt-BR com vírgula, auto-cálculo do total); **cálculo de consumo (Regra de Ouro #2)** — primeiro abastecimento sem baseline, dois cheios = 12,5 km/l, parcial+cheio com janela = 10,0 km/l; aviso não-bloqueador de odômetro retrocedendo; editar/soft-delete de abastecimento com recálculo automático | ✅ **Aprovado** (após fix) | **A**: telas de form (criar/editar veículo e abastecimento) não tinham botão voltar | Causa-raiz: navegação usava `context.go` (substitui rota) em vez de `context.push` (empilha). Fix: switched para `push` nos caminhos lista→form + `leading: BackButton` explícito nas AppBars dos forms + save com `canPop?pop:go`. 4 testes de regressão. Re-homologado e aprovado |
| 2026-05-23 | Sprint 3 | Edge Function deployada (Claude Haiku 4.5 via Supabase, chave nunca no app); fluxo de scan completo no simulador iOS via bottom sheet câmera/galeria; pré-preenchimento PT-BR com decimais por vírgula; banner "Dados extraídos do cupom. Revise antes de salvar."; salva com `source = ai_scan`; cota incrementa em `usage_quota` (só quando IA entregou liters+price); 6º scan → banner "Cota esgotada" + CTA placeholder Premium; validação bloqueante de odômetro (initial + cross-date estrito + mesma-data relaxado, sem hora); ordenação semântica `date DESC → odometer DESC → createdAt DESC`. **3.4 (ML Kit OCR odômetro) adiada** — não roda no simulador iOS Apple Silicon | ✅ **Aprovado** (após 6 fixes) | **A1**: banner "Dados extraídos" não dismissava no OK. **A2**: lista desordenava quando datas eram iguais. **A3**: limite de imagem 1.5MB curto pra cupom real. **B1**: faltava validação cronológica cruzada (form só comparava com último, não respeitava posição no meio). **C1**: aviso era fraco, queria bloqueio. **C2**: tiebreaker por createdAt era anti-intuitivo, queria por odômetro | A1: `ScaffoldFeatureController.close()` em vez de `hideCurrentMaterialBanner` (aplicado nos dois banners). A2: tiebreakers `odometer DESC → createdAt DESC` no orderBy. A3: bump 1.5MB→5MB + compressão 1280/q80 nativa. B1+C1: função pura `validateOdometerForEntry` bloqueante (initial check + cross-date estrito + mesma data sem check) + botão Salvar desabilita + mensagem PT-BR inline. C2: orderBy secundário por odômetro DESC. PRD §7 atualizado refletindo nova regra. ~14 testes de regressão somados. Re-homologado e aprovado |
| 2026-05-24 | Sprint 4 | CRUD UI completo de **despesas** (categorias PT-BR, valor com vírgula formatado R$ X.XXX,YY, lista ordenada); CRUD UI completo de **lembretes** com SegmentedButton porKm/porData + toggle done direto na lista + strikethrough nos feitos; **notificação local** agendada na data alvo pra lembretes porData; **disparo automático** de lembretes porKm quando odômetro cruza o alvo via novo abastecimento; novos icons na AppBar do detalhe do veículo (🔔 lembretes + 💲 despesas + ✏️ editar) | ✅ **Aprovado** (após 2 fixes) | **B1**: form de lembrete `porKm` permitia salvar com alvo abaixo da km atual (dead-on-arrival — trigger nunca aciona). **B2**: pencil de "editar" nas listas de lembretes/despesas levava ao edit do **veículo** (afundance confuso fora do contexto) | B1: nova função pura `validateDueKm` bloqueante; Save desabilita + mensagem PT-BR inline "Quilometragem alvo deve ser maior que a atual (X km)"; 8 testes regressão. B2: removidos os ícones ✏️ das AppBars de lembretes e despesas; edit do veículo continua acessível pela tela de detalhe (fuel history). Ideias futuras anotadas em BACKLOG: validar dueDate>hoje, lembretes periódicos km/data, despesas recorrentes auto-virando lembretes |
| 2026-05-24 | Sprint 5 | 3 agregações puras testadas (gasto/mês, consumo médio/mês ponderado por km, preço/litro/mês ponderado por litros — todas Decimal sagrado); tela `/vehicles/:id/reports` com 3 Cards + LineCharts via fl_chart; novo ícone 📊 na AppBar do detalhe do veículo (ordem final: 🔔 💲 📊 ✏️); reatividade via providers compondo `AsyncValue` dos streams de fuel/expenses existentes; labels PT-BR ("mai/2026") via tabela hardcoded (sem dependência de `initializeDateFormatting`); estado vazio "Sem dados suficientes ainda." por card | ✅ **Aprovado** | — | — |
| 2026-06-04 | 6.GG (re-homolog) | Onboarding após desinstalar/reinstalar app deveria aparecer antes do login | ❌ Reprovado → Corrigido | **B**: gate exigia `isLoggedIn=true` antes de mostrar onboarding; `auth_redirect` bloqueava `/onboarding` pra não-logados; `onboardingNeededProvider` dependia de userId (Drift) | Caso B (spec errado). Inverteu lógica do gate: agora `seen` ou `isLoggedIn` curto-circuitam pra false; `!seen && !loggedIn` mostra. Adicionou `publicRoutes = {'/onboarding'}` no `auth_redirect` para não barrar pré-login. Persistência migrada para SharedPreferences (`onboarding_seen`, sem userId). `onboardingNeededProvider` reescrito independente de auth/userId. Router passa a checar onboarding ANTES do auth gate. UI: "Começar" virou "Criar conta" (primário, → /signup) + "Já tenho conta" (text, → /login); "Pular" → /login. Coluna `onboarding_seen` em `user_settings` marcada `@Deprecated` (morta, anotada em BACKLOG). Teste de regressão adicionado: `seen=false && !loggedIn → true`. 1225 testes verdes, analyze limpo. Re-homologação pendente. |
| 2026-06-04 | 6.GG (re-homolog #2) | Após fix do gate (c60065f), reinstalou app e ainda caiu no /login sem ver onboarding | ❌ Reprovado → Corrigido | **A**: race condition — redirect lia `FutureProvider.valueOrNull` em cold boot; future ainda não tinha resolvido → null → caia no auth gate → /login antes do onboarding gate atuar | Caso A (bug de implementação). Pré-carrega `SharedPreferences` no `main()` antes do `runApp` via `await SharedPreferences.getInstance()` + override em `ProviderScope`. `sharedPreferencesProvider` criado como `Provider<SharedPreferences>` com `UnimplementedError` sem override (força boot correto). `OnboardingRepository.hasSeenOnboarding()` agora síncrono (recebe prefs no construtor). `onboardingNeededProvider` migrado de `FutureProvider<bool>` para `Provider<bool>`. Redirect em `app.dart` lê valor pronto sem `.valueOrNull`. `authIsLoggedInProvider` criado como `Provider<bool>` overrideable em testes. 4 testes de regressão cold boot adicionados (sem await). `widget_test.dart` atualizado com override de `sharedPreferencesProvider`. 1231 testes verdes, analyze limpo. Re-homologação pendente. |
| 2026-06-04 | 6.GG (re-homolog #3) | "Criar conta" / "Já tenho conta" no último slide entravam em loop infinito (botão primário aparecia como ")" — spinner preso) | ❌ Reprovado → Corrigido | **A**: `Provider` cacheia `onboarding_seen`; após `markSeen()`, redirect lia valor antigo → mandava de volta pra /onboarding → loop | Caso A. `ref.invalidate(onboardingNeededProvider)` entre `markSeen()` e `context.go(dest)` em `_markSeenAndNavigate`. Teste de regressão adicionado. 1232 testes verdes. Re-homologado e aprovado. |
| 2026-06-04 | 6.L (chip IA) | Chip "Preencher com IA" em VehicleForm aparecia branco em fundo creme no light mode — invisível | ❌ Reprovado → Corrigido | **B**: `labelStyle` do `ChipTheme` global herdava cor via cadeia `DefaultTextStyle` do `ExpansionTile` ancestral, vazando branco no light | Caso B. Styling explícito: fundo `AppColors.brand.withValues(alpha: 0.10)`, borda 0.25, ícone+label `AppColors.brand`, `labelStyle` via `AppTypography.body(13, w600)`. Consistente em ambos temas. Aprovado. |
| 2026-06-04 | 7.3 (dialog dark) | AlertDialog "Excluir conta" abria com fundo branco + texto invisível no dark mode | ❌ Reprovado → Corrigido | **A**: `surfaceTint` do Material 3 sobrescrevia `backgroundColor` do `DialogTheme` global; textos herdavam branco → invisíveis em fundo branco | Caso A. `backgroundColor: colorScheme.surface`, `surfaceTintColor: Colors.transparent`, `titleTextStyle`/`contentTextStyle` explícitos com `colorScheme.onSurface` nas 2 etapas. Guardrail global em memory `theme-dual-check.md`: toda UI nova/fix passa por validação light+dark antes de fechar. Aprovado. |
| 2026-06-04 | Onda 1+2+3 (consolidação) | Após 3 fixes acima, validou em device iOS: onboarding pré-login, CSV export, PDF histórico, filtros e busca fuel_history, comparar período, CO2 card, lembretes recorrentes, LGPD exclusão (cancelado), skeleton loading, haptic, paging fuel_history (>25), Apple Sign In (botão presente só iOS, tap dá erro esperado), migration schema v16→v18 sem crash | ✅ **Aprovado** | — | Aprovação consolidada das mudanças acumuladas desde 26/05 (commits `fb9f960` em diante: TTS 6.CC, Calendar 6.EE, revert WhatsApp 6.FF, polish-3, onboarding 6.GG, CSV 6.II, Apple 6.LL, comparar 6.KK, filtros 6.JJ, CO2 6.CC, LGPD 7.3, PDF 6.Y, recorrentes 6.MM + 3 fixes do dia). 1232 testes verdes, analyze limpo. |
